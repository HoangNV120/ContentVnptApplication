@model ContentVnptApplication.Models.Banner

@if (Model != null)
{
    <div class="main-banner">
            <img src="@Model.ImageUrl" alt="@Model.Title" style="width:100%; height:200px; object-fit:cover;" />
    </div>
}